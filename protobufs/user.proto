syntax = "proto3";

package user;

service UserService {
  rpc createWithUsername (CreateWithUsernameParams) returns (UserId) {}
}

message UserId {
  string id = 1;
}

message CreateWithUsernameParams {
  string username = 1;
  string password = 2;
  string nickname = 3;
}
